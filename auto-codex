#!/usr/bin/env node

const { main } = require("./src/cli");

main().catch((err) => {
  const message = err && err.message ? err.message : String(err);
  console.error(`auto-codex: ${message}`);
  process.exit(1);
});
